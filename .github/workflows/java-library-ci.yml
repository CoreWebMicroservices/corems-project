name: Java Library CI

on:
  workflow_call:
    inputs:
      java-version:
        description: 'Java version to use'
        required: false
        type: string
        default: '25'
      run-tests:
        description: 'Run tests'
        required: false
        type: boolean
        default: true
      publish-artifacts:
        description: 'Publish to GitHub Packages on main branch'
        required: false
        type: boolean
        default: true
    secrets:
      github-token:
        description: 'GitHub token for package access'
        required: true

permissions:
  contents: read
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Maven with GitHub Packages
        uses: CoreWebMicroservices/corems-project/.github/actions/setup-maven@main
        with:
          github-token: ${{ secrets.github-token }}
          github-actor: ${{ github.actor }}
          java-version: ${{ inputs.java-version }}

      - name: Build and Install
        run: mvn clean install -DskipTests=true -s ~/.m2/settings.xml
        env:
          GITHUB_TOKEN: ${{ secrets.github-token }}

      - name: Run Tests
        if: inputs.run-tests
        run: mvn test -s ~/.m2/settings.xml
        env:
          GITHUB_TOKEN: ${{ secrets.github-token }}

      - name: Publish to GitHub Packages
        if: inputs.publish-artifacts && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: mvn deploy -DskipTests=true -s ~/.m2/settings.xml
        env:
          GITHUB_TOKEN: ${{ secrets.github-token }}
